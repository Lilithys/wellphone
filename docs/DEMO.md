# 1–2分钟实拍：命令行任务 → 副屏抖音发送

> 下文是已撤回的自动流程演示方案，不是当前操作指引或通过记录。当前根入口已接回旧监督执行器，需要操作者看图给提案，见 [当前入口说明](BASELINE-ENTRY.md)。不可按下文宣称无人值守完成。

这是待实拍指引，不是已通过记录。新无审批流程尚待真机验收；过去的数字草稿输入不等于发送成功。

## 开始前

检查依赖、密钥、目标现有一对一会话及旧任务记录。主屏进入无敏感内容的短信草稿并保持键盘；不打开抖音。结束其他工作区的手机任务，用另一台摄像机同时拍手机物理主屏和电脑副屏，避开密钥与无关私聊。

先检查目标解析：
```sh
python3 run.py '在抖音给“联系人昵称”发送“1”' --dry-run
```

再用真实昵称运行，命令本身授权实际发送：
```sh
python3 run.py '在抖音给“联系人昵称”发送“1”' --serial YOUR_DEVICE_SERIAL
```

## 镜头

1. 展示命令中的目标和内容，说明手机与副屏属于同一实体设备。
2. 用户在主屏持续打字；电脑副屏自动判断首页、进入消息列表、寻找目标、输入和发送。流程无审批/观察问答，不要手动帮忙点击。
3. 终端显示实际结果与清理状态。如果模型识别新增己方消息，说明这是界面结果核验；如任务结束后另查接收端，应注明是独立收件观察。

任何阶段停止都展示真实状态，不能说已经发送；已有发送尝试而结果不明时不直接重跑。不要把多轮尝试拼成连续一次成功；若因推理延迟超过2分钟，可明确标注剪辑/加速并保留完整原片。

公开前遮挡无关个人信息。视频后期遮挡不代表模型上传前已脱敏，截图仍是发送给模型的副屏原图。原始日志与私聊截图不提交Git。
