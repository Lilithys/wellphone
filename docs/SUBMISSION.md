# 提交前清单

> 最新状态（2026-09-10）：根入口使用旧监督执行器＋联系人参数；一次完整非展示屏流程已真机通过，见 [当前入口说明](BASELINE-ENTRY.md)。该结果是监督式本机外发，不等于无人值守、长期稳定或对方收件回执。ZIP和本次 README/说明更新需随 Git 提交。

当前根入口是自然语言单消息任务 → 同机副屏抖音执行；命令授权实际任务，但仍保留首页、输入框、草稿和发送按钮核对。历史日历/设置与 Web 是研究记录，不是默认任务。

## 本次代码状态

已接通自然语言入口、有限任务解析、AutoGLM 观察协议、阶段限制、系统输入框检查、防重日志、发送前后图核验及清理。源码和公开文档中的固定昵称已匿名化。2026-09-10 已完成一轮实拍级真机验收：输入和发送均由执行器完成、用户核对主屏正常；不把旧草稿证据与对方收件回执混淆。

## 提交前

1. `python3 run.py tests`、`python3 run.py verify`，再核对 `git diff` 和待提交文件。
2. 用户自行用真实任务进行实机验收并录制1–2分钟视频；现有一次通过记录可作基线，结果不明不补发。
3. 使用普通git提交和推送，先核对远程地址和访问权限，不强推或覆盖旧标签。本次修改没有自动提交/推送。
4. 旧 `dist/*v1*` ZIP/bundle/解压目录未更新，不代表当前源码；重新打包使用新版本名称。
5. 私有outputs、API密钥和截图不进Git。旧包和Git历史仍可能含原昵称；当前工作树替换不是完整历史匿名化。

## 简介（按实机结果补充）

“Wellphone 在同一台安卓手机的隔离副屏上执行命令行明确授权的抖音单消息任务，用户仍可使用主屏。任务解析、阶段约束、原生帧观察、定向 ASCII 输入、持久防重与音频恢复分层实现。2026-09-10 已有一轮完整监督式流程通过：AutoGLM 导航、输入1和单次发送完成，主屏观察正常；不宣称服务端送达、连续火花或通用零干扰。附带部署、离线测试、基线来源和失败复盘。”

旧工作区运行状态和历史标签保留，不因更换源码目录而消失。不要删除防重记录、并行控制同一手机或重放结果未知的发送。
