# 提交前清单

> 最新状态：根入口已撤回新自动流程，改用旧监督执行器＋联系人参数，见 [当前入口说明](BASELINE-ENTRY.md)。下文的自动化提交口径已过时，不能据此宣称完整自动发送验收通过。ZIP和Git提交尚未更新。

当前根入口是自然语言单消息任务 → 同机副屏抖音自动执行；命令即发送授权，无逐步审批及屏幕观察问答。历史日历/设置与Web是研究记录，不是默认任务。

## 本次代码状态

已接通新入口、有限任务解析、自动观察协议、阶段限制、系统输入框检查、防重日志、发送前后图核验及清理。源码和公开文档中的原固定昵称已匿名化。新自动发送只做了离线验证，仍需实拍验收，不能把旧草稿证据写成发送已通过。

## 提交前

1. `python3 run.py tests`、`python3 run.py verify`，再核对 `git diff` 和待提交文件。
2. 用户自行用真实任务进行实机验收并录制1–2分钟视频；结果不明不补发。
3. 使用普通git提交和推送，先核对远程地址和访问权限，不强推或覆盖旧标签。本次修改没有自动提交/推送。
4. 旧 `dist/*v1*` ZIP/bundle/解压目录未更新，不代表当前源码；重新打包使用新版本名称。
5. 私有outputs、API密钥和截图不进Git。旧包和Git历史仍可能含原昵称；当前工作树替换不是完整历史匿名化。

## 简介（按实机结果补充）

“Wellphone 在同一台安卓手机的隔离副屏上执行命令行明确授权的抖音单消息任务，用户仍可使用主屏。任务解析、阶段约束、原生帧观察、定向ASCII输入、持久防重与音频恢复分层实现。新入口无需逐步审批，发送后以画面对比核验新增己方消息，不宣称服务端送达或通用零干扰。附带部署、离线测试、基线来源和失败复盘；自动全流程真机验收状态以实际视频和记录为准。”

旧工作区运行状态和历史标签保留，不因更换源码目录而消失。不要删除防重记录、并行控制同一手机或重放结果未知的发送。
