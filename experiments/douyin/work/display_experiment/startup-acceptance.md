# 非展示屏：本机三轮短时启动预检

2026-09-09，HONOR Magic3 / Android 14，设备 AYYKVB1809001850。状态：三次独立启动预检通过；允许进入人工监督的模型消息导航实验，**不是长期稳定性证明，不是完整抖音发送基线**。

同一服务端 SHA-256：`556bebcba8c260956b674750dc7a10c1c52af2740616703142caf28c04466542`。

- display67：`outputs/douyin-20260909-142001-jc7_i093/result.json`，两次窗口诊断间隔约8.40秒。
- display68：`outputs/douyin-20260909-142418-41nsvd2c/result.json`，间隔约7.58秒。
- display69：`outputs/douyin-20260909-142547-fbg37762/result.json`，间隔约13.59秒。

以上路径相对仓库根目录，原始记录及副屏图片仍在忽略目录，不复制或提交私聊图片。时间间隔只是两次诊断的时间差，不等于连续视频/打字的实测时长；不能把第三轮记作30秒稳定性验收。

共同证据：`display_variant=non_presentation_trial`、实际flags为`0x1fc0`、PRESENTATION mask为`0x8`且未置位；原焦点标记检查通过；开始及结束前的抖音Activity/焦点窗口均匹配，窗口可见且有Surface。用户确认导航完整、主屏输入正常、无额外干扰、背景音乐正常；设备查询/主屏采样无错误，音频恢复及副屏销毁均确认。三轮均未调用模型、未尝试输入或发送。

旧版曾在display66无模型启动中复现缺少导航、`FOCUSED_WINDOW_MISMATCH`。该对比支持非展示屏方案继续测试，但未记录异常窗口实际类型，且旧版也偶尔正常，不能确认Presentation是唯一根因或统计上证明问题消失。

分段测试：`--startup-only --confirm-home-first --non-presentation`，保留副屏实际标记验证、人工首页门槛、原生新帧/主屏/音频检查和一次消息点击限制。运行时须明确同意副屏截图上传；此模式只到消息列表，不进会话、不输入、不发送。后续显式 `--send-one-flow --confirm-home-first --non-presentation --auto-messages` 已作为完整发送实验入口接入，不是新的验收结论。

14:31 display70 首次模型接力：完整导航可见，模型定位底部消息，但点击前被帧差检查拦截，`action_attempted=false`。离线图片对比确认外层导航左上微移2原生像素；对应检查修正在启动脚本层，服务端二进制未变。主屏/音频观察、清理及恢复均正常；此轮不能计为消息导航成功，仍待修正后的真机验收。

14:39 display71：正常首页已人工确认，模型提出消息Tap，人工确认目标后在点击前因3像素导航微移及轻微色差被v1检查拦截。`action_attempted=false`，音频恢复/副屏移除及主屏/背景音乐观察正常。当前启动脚本使用人工确认目标余量的有界RGB结构匹配v2，服务端二进制未变；两次失败帧已离线复核通过，但仍没有真机消息导航成功证据。

14:47 display72：一次真实消息Tap已分发，用户观察及`handoff-after-model.png`证明已进入消息列表；首个点击后的原生图及第二次模型输入图仍停在首页，导致模型建议重复Tap并被拒绝。整轮`passed=false`，不能补记成完整自动测试通过；但它已证明本次导航动作能够生效，主屏/音频/清理正常。后续新增`--auto-messages`取消预览及点击前确认、增加被动转换等待，服务端不变；该新模式仍待真机验收。

15:04 display73：一次消息Tap、两次转换观察后，第二次模型描述真实消息列表；仅因自然语言finish缺少精确标记而在`STARTUP_VALIDATE`停止。主屏/背景音乐观察、音频恢复和副屏移除正常，无输入/发送。后续只增加人工消息页核验兼容及有限单数字接续，不改旧`passed=false`记录，不把模型描述当作送达证明。完整单数字流程尚待真机验收。
