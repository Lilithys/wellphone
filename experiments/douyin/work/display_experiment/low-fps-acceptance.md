# 低帧率启动对照：单轮真机验收范围

2026-09-09，现有HONOR Magic3 / Android 14，display81。只验收非展示屏、人工首页门槛、自动消息入口及低帧率启动器组合；不是完整发送基线。

- 本地记录：`outputs/douyin-startup-20260909-193847-61_44qxo/result.json`，SHA256 `b3e0781334b08399223b0208c42d3fc5754df4ac1fa6f31d365fd0e9d4630a9d`。截图和原记录含私聊信息，仍留在忽略目录，不复制到Git。
- 请求副屏编码上限5fps，原生1080×2400、冻结解码器不变。启动器`scrcpy-low-fps` SHA256 `d5ece853b672480da67f226d7d634528e54af505f5b8173b62414f8da8ae3f28`。
- 一次实际消息Tap `[696,965]`，没有重点击；两次转换观察在约1.77秒、4.24秒记录旧导航改变，转换检查共4.245秒。该时间是执行器观察时长，不是端到端采集延迟。
- 第二次模型请求所用的`startup-model-02.png`已为真实消息列表；模型自然语言finish经用户输入`messages`确认。共两次模型请求。
- 主屏采样错误为空；人工键盘正常、无额外干扰、背景音乐正常。音频限制回读、恢复回读、副屏移除均通过；总结果`passed=true`。
- 没有进入会话、输入或发送。未验证实际编码帧率、源PTS/采集时间、长期无积压、其他设备或完整单数字发送。

与display80“实时窗口已切页但连续PNG仍为首页”相比，本轮支持降低视频负载继续实验的选择；不同轮次的视频内容和状态也有差异，不能断言已证明唯一根因。后续显式`--send-one-flow --low-fps-trial`仅复用这一启动路径，输入/发送仍走原来的身份、空白框、输入结果及最终发送核验；不删除防重复记录，不默认启用，不把本次启动验收当成写入授权。
