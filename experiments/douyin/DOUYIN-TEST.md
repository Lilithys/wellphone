# 抖音小号单数字实验（只输入并发送 1）

从 `baseline-hybrid-agent-v1` 独立分支，原设置/日历执行器不变。复用同机虚拟显示、焦点补丁与原生帧。用户已取消表情任务，改为给同一小号 `示例联系人` 发一条数字 `1`。副屏抖音输入和完整发送均**尚待真机验收**，不把设置搜索框 ASCII 成功推广到抖音。

## 低帧率启动对照已单轮通过（不输入或发送）

19:38 display81真机记录 `outputs/douyin-startup-20260909-193847-61_44qxo/result.json`：一次消息Tap后，两次观察均见导航改变，转换检查4.245秒通过；第二次模型图确为消息列表，用户`messages`核验通过。主屏打字/声音/背景音乐正常，音频恢复、副屏移除通过，没有输入或发送。见[单轮验收范围](work/display_experiment/low-fps-acceptance.md)。此结果支持继续低帧率实验，不证明长期无积压或完整发送已通过。

19:29 display80记录 `outputs/douyin-flow-20260909-192908-8fb77kpn/result.json`：实际分发一次消息Tap后，20秒内9次原生PNG观察仍匹配旧首页；用户确认实时scrcpy窗口确实进入过消息列表。主屏打字/声音/背景音乐观察正常，音频恢复通过，无输入或发送。不能据此重点击或把这一轮改记完成。

代码排查发现：电脑显示消费者可跳过旧帧，录制消费者则逐包排队，经MKV管道交给单线程ffmpeg逐帧生成原生PNG。PNG内容变化、文件刚写入均不能证明设备采集时间。积压是有依据的怀疑，但历史记录没有源PTS，不能量出本轮延迟或断言唯一根因。本地仅RGB→PNG的60帧吞吐约30fps，不含H264解码、颜色转换和磁盘写入，也不是手机实测输入帧率。

保留的只到消息列表对照命令：

```sh
python3 /Users/yishanma/Documents/Codex/wellphone-douyin-experiment/run.py douyin --startup-only --confirm-home-first --non-presentation --auto-messages --low-fps-trial --serial AYYKVB1809001850
```

此开关通过独立启动器给原冻结客户端添加 `--max-fps=5`，请求限制本次副屏视频编码帧率，电脑视频流畅度会下降；不是修改主屏刷新率。`exec`保留进程PID、既有参数和环境，退出仍使用原清理/音频恢复流程。原生1080×2400分辨率、解码器、首页一次点击上限、帧连续性/主屏/音频检查全部保留；不弹预览、不重点击。当前允许上述启动测试，或下节明确的完整单数字实验；两者须选其一，仍不兼容预检/逐步确认。去掉开关即使用原客户端入口，不改冻结基线。

人工首页确认后，观察实时窗口是否进入消息列表，随后看模型是否也识别到消息列表；仍按真实观察回答最终提示。`result.json.stream_trial`记录请求5fps但明确标为实际帧率、采集时延未验证；`scope`区分只到消息列表与监督式单数字实验。启动成功只能说明该次低负载条件下导航及核验通过，不能称为已实现“最新帧保证”。上述`--startup-only`命令失败时仍不进入会话。

初版10项离线测试覆盖启动参数组合、禁止隐式发送路径、默认入口不变、启动器不可用不回退、PID/参数/保护环境保留及记录不虚称时延验证，当时共454项通过。接续版本额外验证显式完整流程的选择、输入/发送取消、单次写入与清理，以及原启动命令不意外进入会话。离线测试不当作真机发送验收。

## 当前下一步：低帧率同一次副屏寻找小号并发送1

```sh
python3 /Users/yishanma/Documents/Codex/wellphone-douyin-experiment/run.py douyin --send-one-flow --confirm-home-first --non-presentation --auto-messages --low-fps-trial --max-steps 20 --serial AYYKVB1809001850
```

这是在单轮启动验收后接续的低帧率完整实验，不是已验收发送基线；不能同时加 `--startup-only` 或 `--preflight`。必须在有 `PHONE_AGENT_API_KEY` 的交互终端运行。最多20次请求（首页阶段最多8次，后续只用剩余额度），不重试API。仍会上传副屏好友列表/私聊画面到已声明的模型服务；不上传主屏。执行器仅新增此参数组合及对应报告，不改联系人范围、截图阈值、输入方法、身份核对或防重复记录。

同一个副屏、同一音频保护和设备锁贯穿全部阶段，不在消息页重新启动抖音。`FIND_RECIPIENT → CHAT → READY_SEND → SENT_ATTEMPTED`：最多6次列表内向上滑动、只打开一个候选会话、最多聚焦一次、定向输入一次1、发送最多一次。模型缺少动作标签时，仅由当前阶段及限定坐标区域分类，不是独立识别联系人或按钮语义；坐标不改写。目标不见/错人、页面变化、旧草稿、输入框归属不唯一、主屏/音频异常均停止，不搜索、不换人、不清草稿、不调用通用Type。候选/输入/发送仍使用原来的严格画面与显示归属检查，不套用首页导航的微移放宽。

这轮不调用图片预览程序、不逐步询问滑动和普通导航，请直接观察电脑上的实时副屏。按实际观察在终端回答，不能预先连续输入批准：

1. 初始授权 `yes`；广告自然结束且首页完整，输入 `1`。
2. 已到真实消息列表时，输入 `messages`，才进入寻找目标阶段。
3. 会话确是自己的示例联系人、已聚焦空白聊天输入框、没有旧草稿时输入 `type 1`。此时才真正输入数字。
4. 副屏框内完整内容恰为1、主屏没有混入1、尚未发送，输入 `yes`。
5. 再核对收件人/内容/发送位置，输入 `send 示例联系人` 才点击发送。
6. 看见新增一条己方1、无发送中/失败标记，再输入最后的 `yes`。这是人工结果核验，不是服务端送达证明。

若输入后停止，可能已有草稿；不会自动清空。若发送结果不确定，不得重跑补发。旧 `douyin-input-one-*` / `douyin-send-*` 记录继续阻止重复，不迁移、不删除。测试结束仍恢复音频并检查主屏打字/背景音乐；任一失败不记整轮通过。

15:04 display73记录：`outputs/douyin-startup-20260909-150417-nz_gxdtg/result.json`。一次消息导航后观察到画面转换；第二次模型描述了消息列表，但未写精确 `MESSAGES_READY`，因此原轮失败，未输入或发送。新兼容仅在本次已尝试消息点击、观察到转换且上下文仍匹配时，把自然语言finish交给人工消息页核验；`startup_finish_reviews`明确记录非自动成功，未确认不继续。不会把会话阶段的自然语言finish当作发送完成。

444项离线测试通过，93个冻结文件一致：覆盖自然语言结束语的批准/拒绝、有限会话动作、输入/发送取消、单次持久记录、完整入口共享预算与清理、音频恢复失败不能报成功、一次未执行提案的重新定位、分阶段真实请求组装及候选行区域校验。使用假设备/模型和合成图，**这不等于真机抖音输入/发送已通过**。本轮开发没有操作手机或请求模型。

### 20:00 副屏真实焦点已确认，动作示例回显导致停止

记录`outputs/douyin-flow-20260909-200032-x5613919/result.json`（display83）：再次单击消息入口、两次滑动找到示例联系人、打开会话并聚焦。原`require_editor`确认唯一可见/启用/获焦点的`SearchableEditText`，资源`app:id/msg_et`，客户端UID/用户/副屏与窗口归属均匹配。说明无软键盘不等于没有编辑焦点。

该轮模型复述用户任务中的`finish(message=...)`示例，随后才写Type；冻结的上游解析器优先取首个finish，执行器因此停止。没有输入或发送，一次性输入/发送记录尚未创建；音频恢复、主屏结构检查未报错，真人观察未采集，不能宣布整体验收。`single-stage-v4-protocol-separation`把示例从用户任务移到系统协议并要求不复述，保留原解析器/动作检查，不从歧义输出中选择最后一个动作。尚待真机验证。

### 19:53 助手接管：输入前读取真实焦点，不让模型猜键盘

`outputs/douyin-flow-20260909-195338-vi20cga7/result.json`，display82：首页一次消息Tap、两次列表滑动、进入示例联系人、一次输入框Tap均通过。第7次模型请求把无软键盘/仍有占位文字解释成未激活并大量重复，最后没有有效输入提案；没有触发输入器、输入/发送记录为空，音频恢复和副屏移除正常。主屏结构化采样无错误，但未取得本轮真人手感反馈，记录未观察，不伪记隔离验收通过。

当前`single-stage-v3-editor-evidence`在输入阶段的模型调用前执行原`require_editor`只读检查，保存`conversation_editor_preflight`。仅系统确认唯一可见/启用/获焦点编辑框后，提示模型只核对新图中的示例联系人和空白草稿，不以无软键盘或占位符判断未聚焦。焦点证据不能证明身份或空白；原输入前两次再检查、逐次批准和一次性记录全部保留。系统检查失败时不调用输入阶段模型、不输入、不重放Tap。完整流程每次响应上限由3000缩为1024 token，防止长时间重复；输出不完整仍停止，不修复为动作。修改后472项离线测试通过，93个冻结文件不变，真机输入仍待验收。

### 19:15 行区版本：受限支持行中部，增加左侧行区域校验

`outputs/douyin-flow-20260909-191522-2_ze4law/result.json`，display78：两次滑动后已出现示例联系人，模型第5/6次请求均提出`[499,552]`，一次重新定位也没改变坐标。两张原生图都显示该点位于目标行中部空白，未落到右侧快捷按钮；不能据此声称已实测点击效果。原x≤450限制在分发前拒绝，无会话点击、输入或发送，主屏/设备采样、音频恢复及副屏清理正常。

当前`single-stage-v2-row-context`仅将FIND_RECIPIENT候选Tap范围从x=80–450扩为80–550，y=210–880不变；不将整行或右侧快捷按钮纳入，不改写模型坐标。更偏右550<x≤650仍只允许一次未执行提案重新定位，搜索等其他区域仍拒绝。此取舍针对本机已观察到的列表布局，不是通用App按钮语义证明。

为避免行中部空白让错误行通过点位像素检查，候选Tap在原两次执行前帧检查中同时比较原点击点和左侧行区域：1080×2400原生帧中x=32–640、以点击y为中心上下各72像素（上界半开），逐RGB均值差阈值仍为3，不平移、不忽略头像变化。此开关由执行器当前阶段设置，模型不能添加/关闭；进入CHAT后关闭，原输入/发送的身份、草稿、编辑器和标题核验仍保留。它是图像连续性检查，不是独立身份认证，动态头像也可能导致保守停止。

真实第5/6次请求的截图对已离线通过新检查，未操作手机。新增7项离线测试：原始`[499,552]`不改坐标分发、550边界、中心白色但左侧变化的前/后两次拦截、阶段开关隔离、防模型绕过，以及保留一次输入/发送核验的完整假入口。真机会话打开和完整发送仍待验收；不改旧失败记录。

### 19:04 搜索提案被拒：改为逐阶段下达任务

记录 `outputs/douyin-flow-20260909-190416-cf292or5/result.json`，display77：首页→消息列表成功，找人阶段首次请求的截图是列表顶部，目标示例联系人尚未可见。没有滑动，模型直接提出搜索Tap `[813,69]`，在执行前被拒。主屏/设备采样无错误，音频恢复、副屏移除正常，无输入/发送。

旧系统提示已禁止搜索，但每轮用户消息仍给出完整“找人并发送”目标；不能据此断言远端模型忽略了哪个角色消息，但它确实重新选择了未支持的搜索路线。新`single-stage-v1`只向每轮模型提供当前小任务：`LOCATE_RECIPIENT_IN_LIST`（目标不可见则向上滑动）、`LOCATE_EMPTY_CHAT_EDITOR`、`PROPOSE_FIXED_ONE_INPUT`、`LOCATE_SEND_BUTTON`。找人阶段不提供后续Type格式和完整发送目标，输入与发送阶段仍由既有执行器状态及人工核验推进。未知/结束阶段不请求模型。

`model_requests`现记录`prompt_version/substage/task`，便于核查实际组装的任务；不含密钥、请求头或主屏数据，不新存模型推理。搜索点击仍拒绝，不将其改成Swipe、不放宽边界、不增加重试。新增6项离线回归，包括直接使用真实`PhoneAgent.step`与消息构造器、只替换模型响应和设备的四阶段测试，确认模型请求中的任务/图片/阶段一致且仅一次固定输入和发送。它证明本地请求组装，不证明远端模型稳定遵循；新版本尚待真机验收。

### 历史18:53 找人阶段：边界反馈，不放宽点击

记录 `outputs/douyin-flow-20260909-185359-yw_ux4bd/result.json`，display76：进入消息列表并滑动两次，`conversation-model-05.png`已可见示例联系人。模型提出`[499,594]`，在目标行中部，但执行器仅允许x=80–450/y=210–880，原提示只说“左侧头像/昵称”，未提供数值，因此提案在分发前被拒绝。没有候选会话点击、输入或发送；主屏/设备采样无错误，音频恢复及副屏移除正常。不把此轮改记成功。

当时的实现将各阶段数值边界及“任务给出名字不代表已找到目标”发给模型。仅FIND_RECIPIENT阶段、合法Tap结构与导航标签、450<x≤650且210≤y≤880的中部越界提案，可在无任何分发且预算仍足够时重新定位一次。`conversation_relocalizations`记录旧提案未执行；下一请求重新读取并冻结副屏帧，携带拒绝原因，原20次总请求上限不增加。未将x=499裁剪为450或代填坐标；当前范围与额外校验见上。

第二次越界、其他区域/标签、截图尺寸或新帧检查失败仍停止；不重试API异常、已分发点击、输入或发送。新定位的合法点击仍须通过原画面、显示归属、主屏及音频检查。新增8项离线测试覆盖重新定位一次、边界不扩大、新帧/反馈/预算、二次拒绝、新帧/API失败以及完整入口最终只输入/发送一次；尚待真机验收。

## 保留的分段测试：同一次副屏先确认首页，再接入模型

非展示屏版在display67/68/69连续三轮短时启动预检通过，见[验收范围](work/display_experiment/startup-acceptance.md)。display72已观察到一次真实消息导航，但整轮自动核验未通过。以下为分段测试及历史尝试，测试计数按当轮记录保留；不会修改旧失败结果。

### 当前新增：不弹图、不逐步确认的消息入口测试

```sh
python3 /Users/yishanma/Documents/Codex/wellphone-douyin-experiment/run.py douyin --startup-only --confirm-home-first --non-presentation --auto-messages --serial AYYKVB1809001850
```

`--auto-messages`仅允许上述组合，或文首显式 `--send-one-flow` 完整流程，不能单独用于普通导航、预检或`--step-by-step`。保留初始授权及首页人工输入`1`；不再生成/打开红圈预览、不再要求点击前输入`messages`、不增加点击前倒计时。用户持续在主屏打字即可；最终消息列表仍在终端询问一次观察结果，不弹图；完整流程确认后会继续找人。

自动Tap只接受本机已测试竖屏消息槽内区x=680–720/y=955–975（模型0–999坐标），要求本次非展示屏回读、人工首页门槛、同次确认首页帧及最新帧的导航连续性。位置和结构匹配是限定布局的启发式，不是通用语义识别，也不伪记成人工红圈批准；`startup_auto_navigation`和`startup_actions.approval=preauthorized_bounded_navigation`标明区别。原始模型坐标不改写，明确错误标签、其他入口、输入/发送均拒绝；无标签的有限消息候选可自动通过同一检查。只允许一次实际点击，之后任何Tap（含漏标签）明确拒绝。

点击后最多20秒、10次被动截图观察，旧首页导航仍匹配时不发起下一次模型请求；连续两次观察到导航结构变化，才把确切的该帧冻结交给模型。此处的变化**不是消息页语义验证或设备采集时间证明**，最终仍由模型与用户观察共同核验。超时、主屏/音频/上下文/截图异常不重点击；不重试API，不关闭再重开副屏。`startup_transition`记录观察状态；图片仅在本机忽略目录。旧截图和测试记录不删除。

14:47 display72记录：`outputs/douyin-startup-20260909-144713-u9pr46jx/result.json`。实际一次消息Tap成功分发；点击后的`startup-after-01.png`及第二次模型图`startup-model-02.png`仍是首页，稍后`handoff-after-model.png`已是真实消息列表，与用户观察一致。重复Tap未执行；主屏/背景音乐观察、音频恢复及副屏清理正常。模型配置读冻结的原生PNG，不读电脑窗口截图，因此没有证据表明预览窗口遮住了模型画面。页面加载或传输/解码积压仍未区分；本地PNG写入时间不能证明设备采集发生在点击后。新转换等待仅处理过早核验，不把其描述为修复了全部截图时延。

新增16项假设备/合成图测试：无弹图无逐步确认、严格候选范围、首页/副屏保护、一次点击、防重复、转换等待及超时、冻结确切转换帧给第二次模型、入口清理。共402项离线测试通过，93个冻结文件不变；本轮没有操作手机或调用模型。

**当前检查版本为`messages_structure_v2`。** 14:39 display71记录：`outputs/douyin-startup-20260909-143949-c63m5odm/result.json`。人工预览至点击前复查间隔约24.68秒，仍是完整首页；外层导航共同左上移动3原生像素，目标区域校正后的RGB平均差为3.36/2.37/2.10，旧v1的2像素上限及逐通道差3均不足以覆盖该样本。`STARTUP_POST_REVIEW`停止、无实际点击/发送，主屏/音频/恢复/清理正常。微移的系统或应用根因未确认。

v2保留普通严格检查，只有它失败时才进行RGB结构匹配：目标区域及左右两个导航锚点必须以同一偏移匹配，每区域每通道相关系数≥0.98，均值色差≤8、对比度比例0.85–1.15，低纹理及含糊匹配拒绝。不掩盖徽标、不缩放/扭曲、不改写坐标。人工现在须确认半径32原生像素的整个红圈都在消息按钮内；估计位移每轴最多8像素（该半径的1/4），搜索至12像素仅为拒绝最佳匹配落在允许范围外的情况。此范围来自人工确认的目标余量约束，**不是系统无干扰保证或自动按钮语义证明**。批准后仍获取新帧复查，显示/Activity/主屏/音频检查不变；仍只点击一次消息入口。此规则不进入普通导航、输入和发送执行器。

两轮真实失败截图的三组图对已离线复核通过；新增亮度/对比度、3像素位移、越界位移以及完整假入口的确认前后微移→一次点击→恢复/销毁测试。386项离线测试通过、93个冻结文件一致；本轮开发未操作手机或调用模型，不能记为实机消息导航成功。以下v1数值保留为历史尝试，不是当前运行规则。

14:31 display70：`outputs/douyin-startup-20260909-143104-655uqxp4/result.json`。完整导航仍在，模型提出消息Tap `[696,965]`，人工预览之前触发`RegionChanged`；没有实际点击或发送，设备/主屏采样无错误，音频恢复/副屏清理及用户观察正常。两张原生帧离线对比：消息区域原始RGB平均差约13.13；外层导航共同左上移2像素后，候选区差异约1.09，左右导航锚点约0.31/0.13，中间内容未移动。这里只证实帧间微移，不推断系统/应用的具体原因，也不把旧报错中的“收件人标题”当作身份变化证据。

针对此证据，新增`messages_rigid_translation_v1`只用于已人工确认首页的消息接力：保留原像素检查，失败后才比较候选区及左右两个固定导航锚点；必须有足够纹理、共同平移、低差异及明确最佳匹配。搜索半径4像素仅用于发现并拒绝更远的最佳匹配，允许的估计位移仍每轴最多2像素；逐通道差异阈值仍为3。不缩放/扭曲图片，不掩盖未读徽标，不改写坐标、不自动点击。最新图红圈人工核对后，倒计时结束仍要重新截图执行同样检查；较大位移、按钮消失、区域独立移动、匹配含糊或上下文/主屏/音频异常继续停止。`startup_navigation_frame_checks`记录采用微移匹配的阶段和数值，不是语义或发送证明。发送/输入、通用导航以及所有冻结文件均不改。新增20项离线回归（合成图、假设备），并用该真实失败图对离线验证；未操作手机、未调用模型，消息导航等待下一次真机验收。

仍可单独使用 `python3 run.py douyin --preflight --non-presentation --serial AYYKVB1809001850`（去掉 `--non-presentation` 即原版）：启动后保存 `preflight-start`，用户回答后保存 `preflight-before-close`，然后照常清理；记录在忽略目录，不上传模型。匹配失败记录固定分类 `DISPLAY_SECTION_NOT_UNIQUE` / `ACTIVITY_NOT_UNIQUE` / `FOCUSED_WINDOW_MISMATCH` 等，不接受其他窗口作为替代。

14:11:45 display66 的无模型预检复现了导航消失：`outputs/douyin-20260909-141145-u21a40gw/result.json`。启动及关闭前诊断均为 `FOCUSED_WINDOW_MISMATCH`，Activity仍是抖音，设备命令无错误，音频恢复/副屏销毁完成。这排除了“只有模型模式才会出现”这一解释，但尚未识别实际焦点窗口，不能确认它是Presentation。独立非展示屏对照的依据、构建与回退见[实验说明](work/display_experiment/README.md)。新二进制完成本地编译，357项离线测试通过，93个冻结文件不变；本轮开发未操作手机、未调用模型，尚待实际对照结果。

14:08:55 的 display65 无模型预检通过：`outputs/douyin-20260909-140855-s9z76n5l/result.json`；启动图有完整顶部栏目和底部导航，主屏/背景音乐正常，无设备查询错误，音频恢复及副屏销毁已确认。但旧版没有保留窗口诊断，不能在关闭后补取，也不能据此宣称间歇故障已修复。新版自动留证路径仍待真机验证。

```sh
python3 /Users/yishanma/Documents/Codex/wellphone-douyin-experiment/run.py douyin --startup-only --confirm-home-first --non-presentation --serial AYYKVB1809001850
```

仍须同意副屏截图上传与临时应用音频限制，并在手机主屏保持短信草稿键盘。副屏启动后先暂停，**不要在副屏点击、返回或手动恢复导航**；本轮要保留原始启动状态：

1. 若有明确开屏广告，可留在确认提示处等待广告自然结束，不手动点击副屏。顶部栏目及底部首页/消息/我完整、确为正常首页时，**在电脑终端手动输入 `1`**。程序重新保存当前首页证据，以该首页建立后续校验起点；允许此前同一抖音副屏内的广告Activity自然切换，不要求它和首页同类。确认后的5秒倒计时及模型接入阶段仍严格检查Activity连续性；不重启抖音或另建显示。
2. 广告结束后仍只有视频、缺少导航、不是首页或不确定，输入 `0`。现场截图和可获取的结构化窗口诊断保存到本机；不创建/调用模型，也不要求输入密钥。副屏保留供观察，按回车后关闭并恢复音频，不反复等广告。输入 `q` 可明确取消。这里的数字只供电脑确认，不会输入或发送到抖音。
3. 接力模式只允许一次底部“消息”点击（确认整个红圈都在消息按钮内、距其他控件有余量，再输入 `messages`）及短等待。不允许返回、跳过广告、搜索或会话点击。模型看到导航消失应停止，不依据旧首页盲点。到完整消息列表后再输入 `messages` 完成人工验收，随后退出；不是发送任务。

接力模式的漏标签兼容：仅在已确认首页、尚未尝试消息导航时，形如 `do(action="Tap", element=[696,967])` 的无标签Tap可进入人工核对，而不是自动执行。仅接受原生Tap的必要字段，额外字段、显式错误标签（包括SEND_ONE/导航/null）、无标签的其他动作仍拒绝；无标签坐标须在本机竖屏粗略候选区x=550–850、y=900–999，**区域限制不证明它就是消息按钮**。用户必须核对红圈确为当前底部消息导航，手动输入 `messages`；`yes`、`n`、`1` 或发送批准都不会放行。坐标不改写，原有前后帧/上下文/主屏/音频检查仍执行。`startup_label_reviews` 记录PENDING/HUMAN_CONFIRMED/HUMAN_REJECTED，实际动作另在 `startup_actions` 记录；人工确认不等于动作已经执行。不加 `--confirm-home-first` 的模式仍拒绝无标签Tap。

两种启动模式现在都将执行器记录的已完成动作、连续等待次数和累计等待秒数带入每次模型请求。即使重新构建模型上下文，也不会丢掉执行进度；连续最多2次Wait，第三次在分发前拒绝。取消、未执行的提案和单纯报告HOME_READY不重置等待预算；真实导航成功后才重置连续次数。模型错误仍不重试。

首页确认仍兼容精确的 `home` / `missing`；不认识的输入至多重问一次，不进行模糊匹配或删除控制序列后批准。`home_gate_input_checks` 只记录识别结果、字符数量与控制/格式/非ASCII字符数量，不保存原始输入、转义文本或哈希。`CANCELLED`、`INVALID_INPUT`、`MISSING_NAVIGATION` 分开报告。`pre_confirmation_transition` 记录初始页到用户确认页的上下文变化，只是本次人工确认依据，不是自动广告识别；不放宽应用、显示ID、主屏或音频限制。

新增 `home_gate` / `handoff_diagnostics` 保存 `before-gate`、`home-confirmed`、`before-model`、`after-model`（缺少导航时为 `missing-navigation`）阶段、时间、同一display与截图路径。窗口信息只提取精确匹配副屏抖音的Activity/Window标识和允许的可见性字段；未知格式记为未识别，不猜测、更不保存完整转储或主屏内容。这些字段只是线索，**不能自动判定清屏状态**；诊断不上传模型。`model_requests` 记录每次模型请求对应的副屏帧与可信执行进度。查询/主屏保护异常仍停止并清理。

12:38 真机启动专项只有视频，3次Wait实际完成，第4次动作前查询报错；首次 `launch.png` 已缺少导航，早于模型请求。12:44 `--preflight` 初始帧有完整首页，隔离及音频观察通过。两次使用同样分辨率/焦点标记/启动组件，均报告SplashActivity。用户确认两次之间未手动操作抖音；导航差异根因仍未知。同屏接力用于减少两次启动初始状态不同的影响，不能仅凭前后变化推断模型因果。

本轮离线验证：316项测试通过（新增24项），93个冻结文件哈希一致。覆盖同一副屏人工首页确认早于模型构造、无导航时不需密钥/不调用模型、限定窗口字段与隐私边界、真实执行进度重建、连续Wait上限、一次消息导航、模型与诊断报错后的恢复路径。使用假设备/假模型；本轮开发未操作真机或调用云模型，新接力模式尚待真机验收。

13:38 真机反馈 `outputs/douyin-startup-20260909-133829-uz0k9roj/result.json`：初始诊断为SplashAdActivity，用户观察广告结束后为正常首页；旧确认入口记为CANCELLED、`model_called=false`，主屏和音频观察正常且恢复成功。旧版未保存输入特征，不能确定为何终端显示home却未识别，普通首尾空白会被strip移除，不能直接归因于空格。另发现旧校验错误要求初始广告页与确认后的首页同Activity，上述新版已仅在模型前人工确认边界修正，后续动作检查不变。

确认入口修正后：323项离线测试通过（新增7项），93个冻结文件一致。覆盖数字/原有文字选项、未知输入有限重问、不保存输入正文、模型前同屏广告→首页切换，以及确认后Activity变化仍阻止接入。代码已验证，修正后的真机接力尚未验收；未由开发脚本操作手机或发起真实模型请求。

13:44 真机记录 `outputs/douyin-startup-20260909-134452-8b1gcfr6/result.json`：display61首页确认通过，模型接入后识别完整首页，提出无标签Tap[696,967]。旧启动校验在STARTUP_VALIDATE拒绝，`action_attempted=false`、`send_attempted=false`，主屏采样无错误，音频恢复回读成功；尚未验证消息页到达。漏标签兼容修正后332项离线测试通过（新增9项），93个冻结文件一致，覆盖人工确认前绝不点击、取消、范围/字段限制、帧变化、防重复，以及假模型完整接力与恢复；新版尚待真机验证。

14:00 真机接力记录 `outputs/douyin-startup-20260909-135945-jkhtmpii/result.json`：display62 的模型输入截图有完整首页外层导航，中间图文内容是一张聊天截图。模型将内容误认作当前聊天，提出无标签Tap[99,965]（底部首页），动作校验拒绝；`action_attempted=false`、`send_attempted=false`、`device_command_failures=[]`，音频恢复、主屏/背景音乐观察正常。不是本轮ADB故障，也没有进入会话。

针对此误判，接力提示词版本 `outer-chrome-v1`：定位阶段只识别外层底部消息入口，明确禁止先点首页，不将视频里的聊天/未读徽标当作当前会话；点击尝试后转为只核验真实消息列表，不再提出Tap。阶段由执行器的 `messages_click_attempted` 决定，记录在 `model_requests[].navigation_stage`。不改截图/坐标、不新增点击或重试、不改变执行器的原有人工复核及隔离检查。离线测试只能检查阶段传递和拒绝规则，不能证明模型视觉误判已解决，仍待同一命令真机复测。

## 保留的启动自动观察模式（不加 --confirm-home-first）

```sh
python3 /Users/yishanma/Documents/Codex/wellphone-douyin-experiment/run.py douyin --startup-only --serial AYYKVB1809001850
```

先在已导入 `PHONE_AGENT_API_KEY` 的终端运行；同意音频限制和副屏截图上传后，主屏回到短信草稿持续打字，不在主屏打开抖音。此入口不加载输入/发送动作处理器、不创建或清除一次性输入/发送记录；不进会话。进入消息页本身可能改变该页已读/未读展示，不承诺应用数据完全零变化。

- 仅有明确广告/加载证据时才建议等待，每次最多5秒，连续最多2次；纯视频不默认当广告。每次点击/返回都展示副屏预览：只允许明确的“跳过广告”（输入 `skip`）、底部首页（`home`）、底部消息（`messages`）或安全退出全屏视频（`back`）。确认动作后有5秒时间回手机持续打字。红圈不是所述目标、可能打开广告/会话、点赞/关注/发送时，不批准。
- `HOME_READY` 须用户看到正常首页的顶部栏目和底部导航，输入 `home`；这是中间状态，不是完成。之后才允许点击底部消息。`MESSAGES_READY` 须用户看到消息列表和底部消息导航，输入 `messages`，随后停止并清理副屏。启动时已经在完整消息列表可直接核对，不绕回首页。页面核对不要求视频像素静止；这是人工语义验收，不是自动 UI 语义识别。
- 最多8次模型请求（`--max-steps` 可进一步减小），模型网络/解析错误不重试。不启用模糊意图自动放行：非法动作、未知标签、Type/Swipe/发送动作均拒绝。
- 最多3次同一副屏、仍属抖音的 Activity 变化后重新观察，预算由启动截图、规划检查和动作回读共用。规划期间变化：旧动作不执行，清空模型历史并用新图重新规划。已批准导航成功后的回读变化：只重新取证，不重放点击。动作调用失败/结果不明不重试。显示ID变化、非抖音、主屏/键盘/输入法异常、音频限制变化、解码器失败均直接停止，不纳入重试。
- `--startup-only` 与 `--preflight` 互斥；后者仍是无模型、只启动检查。数字1入口不自动接入这套重新观察策略，待启动模式验收后再决定整合。
- 日志在 `outputs/douyin-startup-*/result.json`：`secondary_context_samples` 记录副屏 Activity；`capture_context_changes` 保存采图前后上下文和对应帧；`startup_reobservations` / `startup_diagnostics` 记录丢弃理由、是否已尝试动作、重观察次数；`startup-model-*`、`startup-review-*`、`startup-approved-*` 保留本机副屏证据。主屏采样/音频恢复仍按原要求验收，不上传主屏，私聊截图不提交Git。

12:25 真机旧版记录 `outputs/douyin-20260909-122523-z2q3un29/result.json`：启动及模型截图为开屏广告，后续变成另一段全屏视频；首个 Back 因规划后副屏上下文变化而未执行。主屏检查没有错误，人观察键盘/声音/背景音乐正常，音频恢复且副屏清理完成。旧日志未保存变化前后的具体 Activity，不能断言是哪两个页面切换。12:13 保存的模型图有正常首页导航，说明副屏并非固定裁掉导航。上述启动专项针对就绪判断与过期提案，尚待本机复测。

启动专项离线验证：292项测试通过（新增30项），93个冻结文件一致。覆盖同屏上下文变化丢弃旧动作、3次共享观察预算、模型错误不重试、人工首页/消息就绪、禁止输入/发送、导航后只回读不重放、音频恢复失败不得通过。使用假设备/模型和合成帧，本次开发没有操作真机或调用云模型。

## 保留的数字1入口（本轮先不运行）

在之前已导入 `PHONE_AGENT_API_KEY` 的 Terminal 直接运行：

```sh
python3 /Users/yishanma/Documents/Codex/wellphone-douyin-experiment/run.py douyin --message 1 --serial AYYKVB1809001850
```

用户将目标改为自己的小号「示例联系人」。可选加 `--preflight` 只启动、不导航/调用模型/发送；不用必须分两轮。两个入口都须同意临时音频限制：启动前保存、限制并回读抖音 `PLAY_AUDIO` / `TAKE_AUDIO_FOCUS`，退出先关副屏及解码器，确认抖音停止播放再恢复原值。`--no-audio` 本身不是手机静音；脚本不改全局音量、不录音。这是整个抖音应用的限制，测试期间不要在主屏使用抖音。可先播放背景音乐再回短信草稿，继续观察有无暂停/压低。若有广告、登录、权限、音频或键盘异常，停止并记录。

明确同意后，最多 12 次 AutoGLM 调用（无自动重试），只上传本次副屏抖音截图。聊天/列表是隐私数据，模型服务为 `https://open.bigmodel.cn/api/paas/v4`；不上传主屏。没有 key 时在 Terminal 隐藏输入，不贴到聊天里。

- 模型找消息列表 → 唯一的一对一小号「示例联系人」→ 聚焦空白聊天输入框 → 专用定向输入 `1` → 核对后点击发送。可点击/滑动/返回/短等待，不搜索、不打开其他聊天、不选表情。不切输入法、不使用剪贴板、不清空/覆盖草稿、不按回车发送。
- 新输入动作仅接受 `do(action="Type", text="1", message="INPUT_ONE")` 的完整固定协议，不委托上游通用 Type。先展示副屏，由用户确认目标身份、是聊天而非搜索、输入框为空且已聚焦，输入 `type 1`。倒计时后精确匹配副屏 Activity/窗口，按 ActivityRecord ID 查询客户端层级，核对包名、UID、用户、display 和唯一可见/启用/获焦点编辑框；无法确认就停止。使用 `input keyboard -d <本次副屏> text 1`，不要求第二套软键盘。
- 输入后要求新帧，由用户确认“副屏完整内容恰为1，主屏未混入1，尚未发送”，输入 `yes`。未确认则停止，不重输；已输入后禁止继续导航改草稿，仅可等待或核对发送。主屏请持续输入汉字以便发现串屏。
- 默认模型显式标记 `message="NAVIGATE"` 的导航动作自动执行；`message="SEND_ONE"` 的点击展示红圈预览，核对正确小号、输入框恰为1及真正发送按钮后输入 `send 示例联系人`。模型分类不是独立语义校验，截图比较也不证明收件人身份/文字；这是人工监督实验，不是无人值守产品。
- 导航/发送缺少标签时暂停当前步，人工选择 `n` 或 `send 示例联系人`；没有先通过数字1输入核验时不准发送。非法坐标、其他 Type 文本/字段仍直接拒绝，不自动补标签或重试。可加 `--step-by-step` 强制逐步核对。
- 每次模型截图绑定本次副屏 ID 与抖音 Activity，执行前重新读取本次解码器的最新原生帧，检查上下文一致、显示仍存活、主屏及音频保护正常；动作后仍须收到新帧。`Back` 没有坐标目标，不再比较整幅动态视频的像素；`Tap` 目标区域、发送时聊天标题、`Swipe` 区域检查不放宽。Activity 相同并不能证明弹窗/聊天内容相同，这仍是人工监督实验。`message="导航"` 等非约定标签仍需人工选 `n`，不会静默改成批准。
- 输入前、发送前分别持久化一次性尝试记录；发送后不再点击，由用户对比前后画面核验“目标正确、新增一条己方数字1消息、无发送中/失败标记”。这不是服务端送达或火花状态证明，也不是自动语义校验。
- 主屏 Activity、输入法、键盘归属、焦点异常即停止后续动作并清理本次副屏；不会自动抢回主屏。需要最后人工记录声音、弹窗、卡顿和丢字等可感知影响。
- 音频限制、原值恢复回读都是本轮通过的必要条件；背景音乐异常也不能通过，填“未测试”不算音乐连续性证明。正常结束、模型超时、动作拒绝、Ctrl+C 均经过同一恢复流程；USB 断开、强杀或播放未停时可能保留限制并提示恢复。存在未恢复事务会阻止新任务，按 [AUDIO-TEST.md](AUDIO-TEST.md) 恢复，不删除记录、不强制停止应用。
- `outputs/douyin-*` 有私聊截图和结果，默认不进 Git。发送记录 `outputs/douyin-send-*.json` 禁止重发；输入记录 `outputs/douyin-input-one-*.json` 防止重启后重复追加数字。输入后停止可能留下草稿；不自动清空、不删除记录绕过保护，先核对结果再决定后续处理。旧发送记录继续有效，不因任务换成数字而绕过。

离线检查：`python3 run.py tests`；冻结文件检查：`python3 run.py verify`。基线回归仍运行 `/Users/yishanma/Documents/Codex/wellphone-versioned/run.py baseline`，它不会发抖音消息。

设备互斥：`douyin`、`audio`（含恢复）默认与兄弟目录 `wellphone-router`/Web 共用手机锁，同时持有原实验目录锁以兼容旧进程。可用 `WELLPHONE_STATE_ROOT` 指向同一共享根，配置不存在时停止，不新建替代目录。发送与音频恢复 journal 留在原 `outputs`，不迁移或删除。锁只协调使用这套协议的入口，不阻止外部 adb 或原始 baseline 脚本；不要并行运行其他手机测试。

失败诊断：`result.json` 的 `stopped_step` 区分动作校验、人工复核前/后的帧校验、发送记录、动作尝试和回读。区域变化时额外保存 `stopped-XX-before.png` / `stopped-XX-after.png` 及差异指标，只含本次副屏；它们不是收件人语义证明。`model_stop_reason` 保留模型停止说明，不把模型 finish 当作已发消息。Wait 后也要求新帧。诊断不会自动重试或放宽像素阈值。

2026-09-08 21:47 真机记录：副屏成功打开抖音消息页；24 次主屏采样无异常，用户观察正常、无额外声音/弹窗/卡顿，副屏正常销毁。首个原生无标签 Tap 在执行前被旧版严格标签校验拒绝；`send_attempted=false`，没有发送尝试记录。仅证明该次启动隔离通过，未证明会话导航或发送通过。上述协议兼容修正尚待下一轮真机验收。

2026-09-08 22:21 独立音频专项通过：`outputs/douyin-audio-20260908-222101-uxukv_l6/result.json`；实际视频播放、无抖音声音、背景音乐和键盘正常，两项音频设置恢复且副屏关闭。现已将同一音频事务接入 `douyin`（含 `--preflight`）并修复动态视频下 Back 误拦截；接入后的完整发送仍待真机验收，尚未发出表情。离线测试使用假设备和假模型，不当作真机成功证据。

2026-09-09 第一轮优化：共享/旧目录双锁、等待新帧和分阶段停止诊断已接入；242项离线测试通过，93个冻结文件哈希一致。

12:13–12:14 真机记录 `outputs/douyin-20260909-121346-tl09qpqv/result.json`：第一个 Tap 执行前因主屏采样异常停止，`action_attempted=false`、`send_attempted=false`。25条成功状态样本；错误先为主屏 Activity 改变，结束检查又报键盘/焦点未保持。人工报告键盘、背景音乐正常、无其他干扰；音频恢复和副屏清理通过。旧采样器未保留失败当刻状态，不能认定是误报或用户主动切换界面。

数字1版本新增 `primary_failed_checks`：保留失败当刻允许的结构化状态和预期 Activity，不保存主屏文字/截图、不放宽主屏规则、不忽略短暂失败。输入器复用已验证的定向 ASCII 思路，但抖音客户端编辑框格式仍须下一轮真机确认。262项离线测试通过（新增20项），93个冻结文件一致；覆盖固定输入、未知结果防重、假模型输入→发送→音频恢复、失败状态诊断。相关测试使用合成层级和假设备，不是实机发送成功证据。
